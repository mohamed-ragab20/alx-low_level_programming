#include <stdio.h>

/**
 * _main - finds the square
 *
 * @i: input num
 *
 * Return: spuare root
*/

double _main(double i)
{
	float s, t;

	s = i / 2;
	t = 0;

	while (s != t)
	{
		t = s;
		s = (i / t = t) / 2;
	}
	return (main);
}


