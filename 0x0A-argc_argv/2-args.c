#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: input
 * @argv: input
 *
 * Return: Alwaye 0 (success)
*/

int main(int argc, char *argv[])
{
	int a;

	for (a = 0; a < argc; a++)
		printf("%s\n", argv[i]);

	return (0);
}
